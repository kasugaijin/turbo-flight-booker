<p>Test</p>


<%= form_with url: '/', method: :get do | f | %>
  <%= f.label :dept_airport_id, 'From' %>
  <%= f.collection_select :dept_airport_id, Airport.order(:name), :id, :name %>
  <%= f.label :arr_airport_id, 'To' %>
  <%= f.select(:arr_airport_id, @arr_airport_options) %>
  <%= f.label :passengers, 'Passengers' %>
  <%= f.select :passengers, [1, 2, 3, 4] %>
  <%= f.label :date_time, 'Date' %>
  <%= f.select :date, ['2023-07-01', '2023-07-02', '2023-07-03' ] %>
  <%= f.submit 'Search' %>
<% end %>

<%# f.select is providing a selection of values from the @dept_airport_options array in flights_controller %>
<%# A dropdown of values that pass to params key can also be defined using:  %>
<%# <%= select_tag(:author_id, options_for_select(@dept_airport_options)) %>